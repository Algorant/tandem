# Tandem Verdigris TUI theme selector.
#
# This workspace intentionally selects the built-in Verdigris preset without
# copying a full theme file into the repository.
#
# NO_COLOR=1 or TANDEM_NO_COLOR=1 keeps Tandem in the terminal/no-color fallback.
theme = "verdigris"

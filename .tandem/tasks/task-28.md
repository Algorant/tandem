---
id: task-28
type: task
title: "Allow warning-free Epic closure after all children are completed and archived"
state: "in-progress"
priority: "low"
tags: ["protocol", "papercut", "workflow"]
accord:
  status: "rework"
  acceptance: ["An Epic with at least one child and all descendant Tasks/Subtasks completed and archived can complete without the missing-delivered-Accord warning.", "Completion does not synthesize a delivered Accord or copy child evidence; archived parent outcome and ordinary explicitly delivered-parent acceptance follow existing rules.", "Active descendants still prevent completion; empty Epics, noncompleted child outcomes, missing/blocking references and ordinary Task cases preserve their applicable existing checks and warning behavior.", "Native regression tests assert the full matrix of all-completed children, delivered-but-active children, no children, canceled/failed children, ordinary Tasks and explicitly delivered parents.", "Normative documentation states the narrow child-based Epic completion exception and distinguishes archive outcomes from delivery status."]
  claimedAt: "2026-09-11T14:46:57Z"
  deliveredAt: "2026-09-11T14:58:31Z"
  validation: ["$ just dev-check", "$ cargo fmt --manifest-path tandem/Cargo.toml --check"]
  constraints: ["Schedule after task-2 (overlapping app/tasks.rs, protocol/README.md and docs/cli/index.md); do not overlap Workers on those files.", "Protocol owns policy meaning; derive Epic roles from resolved documents, never ID shape. App composes existing hierarchy input and performs writes.", "No new Accord status, no derived persisted delivery, no lifecycle bypass, no adapter or TUI implementation changes.", "Submit plan/design before edits and stop for approval; clarify ambiguous hierarchy/outcome cases through worker_ask."]
  summary: "Implemented the approved child-based Epic closure exception with positive-evidence semantics. Protocol now owns the policy: tandem/src/protocol/diagnostic.rs adds ResolvedDescendant { location, canonical_outcome } plus canonical_resolution_outcome (explicit resolution.outcome only, no legacy/default fallback) and epic_child_hierarchy_completed, which suppresses the missing-delivered-Accord warning only for a resolved Epic with a nonempty descendant set where every descendant is Logs + explicit resolution.outcome=completed. completion_policy_diagnostics now takes resolved role + descendants. app/support.rs adds resolved_task_descendants to walk the coherent Board-and-Logs snapshot; app/tasks.rs passes the resolved role and descendants and leaves active-descendant, blocker, and structural checks untouched. No delivered/accepted status, evidence, or lifecycle transition is synthesized; the eligible Epic keeps accord.status=ready and resolution.outcome=completed. CLI human `tandem complete` now prints app policy warnings to stderr before the stdout result (checkpoint warnings remain separate and appear once). Docs state the exception and the archive-outcome vs delivery-status distinction. Tests: 8 new protocol unit tests and 10 native CLI integration tests in tandem/tests/epic_completion_behavior.rs. Required validations pass on committed tree 4a6138f; working tree is clean. No pushes, merges, branch deletion, or worktree removal."
  evidence: ["An Epic with at least one child and all descendant Tasks/Subtasks completed and archived can complete without the missing-delivered-Accord warning.: Integration test eligible_epic_closes_warning_free_without_fabricating_delivery completes a Subtask then Task then Epic and asserts the Epic `--json` warnings array is empty; the human test asserts stderr has no policy warning.", "Completion does not synthesize a delivered Accord or copy child evidence; archived parent outcome and explicitly delivered-parent acceptance follow existing rules.: Archived `.tandem/logs/task-1.md` after eligible closure keeps `accord.status: \"ready\"` and `resolution.outcome: \"completed\"` with no `delivered`/`accepted` status and no `evidence:`. explicitly_delivered_epic_is_warning_free asserts a delivered Epic keeps empty warnings.", "Active descendants still prevent completion; empty Epics, noncompleted child outcomes, missing/blocking references and ordinary Task cases preserve their applicable existing checks and warning behavior.: epic_with_delivered_but_active_child_is_rejected asserts failure with active child; empty_epic_retains_policy_warning_in_json_and_human asserts one warning in JSON and exactly one on stderr; canceled/failed/legacy child tests assert the warning; ordinary_task_with_completed_subtask_retains_policy_warning asserts a non-Epic keeps it; epic_unresolved_blocker_still_blocks_closure asserts `unresolved blockers`.", "Native regression tests assert the full matrix of all-completed children, delivered-but-active children, no children, canceled/failed children, ordinary Tasks and explicitly delivered parents.: tandem/tests/epic_completion_behavior.rs contains 10 CLI-driven tests covering each listed case plus a legacy-outcome case; `cargo test --test epic_completion_behavior` passed 10/10 in the just dev-check run.", "Missing or unknown canonical outcome facts never qualify (positive-evidence correction).: Protocol unit tests absent_canonical_outcome_never_counts_as_completed and unknown_canonical_outcome_never_counts_as_completed assert the warning is retained; the integration legacy test rewrites resolution.outcome to completion.outcome and asserts the warning returns.", "Normative documentation states the narrow child-based Epic completion exception and distinguishes archive outcomes from delivery status.: protocol/README.md adds the exception paragraph and an explicit statement that resolution.outcome is not an Accord status and missing/legacy completion.outcome is not positive evidence; docs/cli/index.md `tandem complete` section documents the behavior, human stderr shape, and updated warning example.", "Protocol owns policy meaning while app assembles resolved inputs.: The predicate and its input type live in tandem/src/protocol/diagnostic.rs; app/support.rs only reads document location and the explicit resolution.outcome field and app/tasks.rs only supplies the resolved role and descendant slice; no role/ID-shape inference was added."]
  filesChanged: ["tandem/src/protocol/diagnostic.rs", "tandem/src/app/support.rs", "tandem/src/app/tasks.rs", "tandem/src/cli/commands.rs", "tandem/tests/epic_completion_behavior.rs", "protocol/README.md", "docs/cli/index.md"]
  note: "Code and policy matrix passed owner just dev-check/fmt plus an independent native release script covering nested completed/canceled children, active delivery, empty/mixed outcomes, unchanged ready parent Accord, and explicit parent acceptance. One documentation-only correction before ready: protocol/README.md says child-based completion 'performs no lifecycle transition' and 'never rewrites the Epic's own Accord status' without qualification. Completion is itself an archive/lifecycle transition, and an expl"
  updatedAt: "2026-09-11T14:58:31Z"
createdAt: "2026-09-09T21:16:54Z"
updatedAt: "2026-09-11T14:58:31Z"
effort: "medium"
relatedFiles: ["tandem/src/protocol/diagnostic.rs", "tandem/src/protocol/hierarchy.rs", "tandem/src/app/support.rs", "tandem/src/app/tasks.rs", "tandem/src/cli/commands.rs", "tandem/tests/epic_completion_behavior.rs", "protocol/README.md", "docs/cli/index.md"]
assignee: "worker-task-28-797c36dc"
---
## Reproduced baseline
On0.13.1, an Epic with a delivered-but-active child correctly refuses completion for active descendants. After that child is completed/accepted and archived, completing the Epic succeeds but warns that the Epic's own accord.status=ready and complete normally follows delivery.

## Algorant-approved policy
Suppress only the missing-parent-delivery warning when a resolved Epic has a nonempty child hierarchy consisting entirely of completed archived Tasks/Subtasks. Child-based closure is sufficient; no fabricated delivered status or copied evidence is written. Ordinary Tasks retain their existing completion behavior. Active descendants still block closure. Empty Epics and hierarchies containing canceled/failed children keep the existing delivery-warning policy (an explicitly delivered parent may still follow normal warning-free completion).

Implement protocol-owned policy over resolved hierarchy/location/outcome inputs, with the app assembling those inputs. Preserve blockers, structural validation and every other completion check. Avoid invoking completion transitions to synthesize an Epic delivery.

## History
Reported from Pi Epic task-102 after all six children were accepted/archived. Prior title said delivered/accepted interchangeably; this clarified scope preserves the crucial distinction. This Task is now authorized for implementation after an agreed plan.
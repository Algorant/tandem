---
id: task-21
type: task
title: "Make Papercuts an overlapping, fully discoverable Board tag view"
state: "in-progress"
priority: "low"
tags: ["tui", "papercut"]
accord:
  status: "rework"
  acceptance: ["Papercut-tagged active tasks appear in their normal workflow-state views and the dedicated Papercuts view; no exclusive membership partition remains.", "The dedicated Papercuts view lists every matching active record once, including nested matches beneath non-papercut parents, without requiring expansion; parent context distinguishes hierarchy from references.", "Tab counts correspond to matching records under current filters; global totals and filtered totals are visibly distinguished when different; every counted match is discoverable.", "Existing Board State/Epic hierarchy navigation, selection and keyboard/mouse interactions remain correct; flat Papercuts selection opens the correct record and exposes useful parent context.", "Regression and real rendering evidence cover the reproduced4-count/1-row case, hierarchy roles, workflow states, filters, empty lists and normal Board overlap."]
  claimedAt: "2026-09-11T14:34:44Z"
  deliveredAt: "2026-09-11T14:50:58Z"
  validation: ["$ just dev-check", "$ cargo fmt --manifest-path tandem/Cargo.toml --check"]
  constraints: ["TUI display/filtering only: no new document types, task kinds, workflow states, persistence behavior, or protocol reinterpretation.", "Own tandem/src/tui/board/**, state.rs, chrome.rs, mod.rs plus narrow TUI usage docs. Do not change app/CLI/protocol implementation.", "Wait for task-32's overlapping TUI changes to integrate before Worker launch. Submit plan/design before edits and stop for approval."]
  summary: "Delivered the overlapping, fully discoverable Papercuts Board lens. The Board Papercuts subview is now a flat tag projection (one row per active Board task tagged papercut, no ancestor expansion) and papercuts also stay in their own workflow-state tabs. Root cause fixed: state_board_entries_with_hierarchy no longer requires a papercut to be a hierarchy root, and __papercuts now routes to a new flat papercut_board_entries() that is the single selection/count source. Flat rows carry resolved parentId context (role + title via relationship_context, never references); tab labels show filtered/total counts while Board filters are active; Enter or a second click on a flat row opens that record's inline preview instead of an invisible hierarchy expansion. Normal State/Epic hierarchy behavior is preserved. Release TUI rendering against a copy of the parent fixture now shows Papercuts 4 with four rows, TODO 5 including the standalone papercut, and detail 'Subtask of: Ordinary parent (task-1)'. Remaining gap: the i-key overlay/header source (papercuts.rs) is unchanged per scope, so it still lists every tagged doc in self.docs; for protocol-correct papercuts (tasks) its total equals the lens total."
  evidence: ["Papercut-tagged active tasks appear in normal workflow-state views and the dedicated Papercuts view; no exclusive partition: TODO tab count is now 5 (task-1 + three nested papercuts + standalone task-2) and shows '#2 Standalone papercut' as a root; Papercuts tab count is 4. Both were captured in the Herdr release pane. Unit test papercut_lens_lists_every_nested_match_once_and_overlaps_state_tabs asserts TODO=5 and Papercuts=4.", "Dedicated Papercuts view lists every matching active record once without expansion; parent context distinguishes hierarchy from references: papercut_board_entries emits one depth-0 row per matching active Board task in document order; the Herdr pane shows four rows for the reproduced fixture with '↳ Subtask of: Ordinary parent (…' context. papercut_lens_parent_context_uses_parent_id_not_references asserts the parentId row carries task-1 and never task-9 from references, and a referenced-only task gets no parent chip.", "Tab counts correspond to matching records under current filters; global and filtered totals distinguished: BoardSubviewTab now carries filtered count plus unfiltered total_count; board_subview_title renders 'PAPERCUTS 1/2' when they differ. board_subview_title_distinguishes_filtered_and_total_counts and papercut_header_stays_global_while_tab_count_is_filtered assert this, and the header indicator remains the unfiltered Papercuts N.", "Existing Board State/Epic navigation, selection and keyboard/mouse interactions remain correct; flat selection opens the correct record with parent context: Keyboard j/j moved to task-1-3 and Enter set expanded_board_doc_id (preview), not hierarchy expansion; mouse SelectBoardItem(_,2) selected task-12 and a second click previewed it. Normal State tree still shows '#1 Ordinary parent' with '3 active' and expands to the three nested rows; Epic arrangement is unchanged for an epic-free fixture.", "Regression and real rendering evidence cover the reproduced 4-count/1-row case, hierarchy roles, workflow states, filters, empty lists and normal Board overlap: Pre-fix baseline reproduction showed 'PAPERCUTS 4' with one row; post-fix release pane shows four rows. Tests cover multi-role (Epic/Task/Subtask), cross-state parent/child, filters with partial and zero matches (empty-state text), reference-only peers, and overlap in State arrangement."]
  filesChanged: ["docs/tui/index.md", "tandem/README.md", "tandem/src/tui/board/mod.rs", "tandem/src/tui/board/render.rs", "tandem/src/tui/mod.rs", "tandem/src/tui/state.rs"]
  note: "Shared classification correction passes owner tests and actual release pane now shows all4 rows with correct parent detail. Live verification exposed a separate localized navigation defect: while on Papercuts with task-1-3 selected, I externally ran `update task-1-1 --tag papercut --tag focus` in /tmp/tandem-papercut-counts.D7Jlfh. Auto reload silently jumped the selected subview from Papercuts to TODO. Source confirms reload.rs capture uses self.states.get(selected_state) (None for pseudo-subview), then re"
  updatedAt: "2026-09-11T14:58:14Z"
createdAt: "2026-09-06T14:20:33Z"
updatedAt: "2026-09-11T14:58:14Z"
references: ["task-22"]
effort: "medium"
relatedFiles: ["tandem/src/tui/board/mod.rs", "tandem/src/tui/board/render.rs", "tandem/src/tui/state.rs", "tandem/src/tui/chrome.rs", "tandem/src/tui/mod.rs", "tandem/src/tui/reload.rs", "docs/tui/index.md", "tandem/README.md"]
assignee: "worker-task-21-b150bc53"
---
## Confirmed defect
The0.13.1 audit reproduced a normal parent with three papercut Subtasks plus one standalone papercut: CLI finds four, the Papercuts header/tab says4, but only the standalone row is visible. Expanding the ordinary parent in TODO reveals the other three. Counting scans all tagged documents, while Papercuts row projection excludes a non-papercut ancestor before traversing children.

## Algorant-approved implementation direction
Papercuts are an overlapping tag-based view. They also remain visible in their normal workflow-state tabs. The dedicated Papercuts view is a flat matching list, one row per matching active Task/Epic/Subtask, with parent context where relevant; no required ancestor expansion can hide a counted match. References are loose links, never hierarchy. Preserve task/tag taxonomy and existing normal Board State/Epic behavior outside membership corrections.

Task-22 owns future modeling research and consumes this display baseline rather than duplicating or blocking it. This Task is now explicitly authorized for implementation, replacing its earlier exploration-only scope.

## Validation focus
Exercise standalone, Epic-child, Task-child, cross-state parent/child and referenced-only papercuts; filtered/no-filter counts and no matches; keyboard/mouse selection and context/navigation. Parent will compare rendered release panes against the recorded 4-count/1-row baseline.
---
id: task-39
type: task
title: "Add an explicit native checkpoint flush for commit and push workflows"
state: "in-progress"
priority: "high"
effort: "medium"
relatedFiles: ["tandem/src/project/checkpoint.rs", "tandem/src/cli/model.rs", "tandem/src/cli/commands.rs", "tandem/tests/checkpoint_behavior.rs", "protocol/README.md", "RELEASES.md", "tandem/Cargo.toml", "tandem/Cargo.lock"]
tags: ["protocol", "git"]
accord:
  status: "rework"
  acceptance: ["A documented native CLI operation explicitly flushes all pending owning `.tandem/` changes through the existing 0.13.4 checkpointer without creating or altering a Task, Accord, Rule, Decision, or event.", "After an ordinary unpushed non-merge source commit, the flush leaves `.tandem/` clean, preserves the real commit subject, includes pending Task/update/rule/Subtask metadata in that commit, and folds eligible leftover Tandem chores using the existing safety rules.", "Pushed commits and merge commits are never amended. Unrelated staged, unstaged, and untracked files remain unchanged. Metadata-only work has at most one rolling Tandem checkpoint rather than one commit per mutation.", "The operation is idempotent when `.tandem/` is clean and returns a structured checkpoint result including status, commit, amended, consolidated, and failure details consistent with lifecycle checkpoint results.", "Protocol/CLI documentation and focused real-Git coverage define the adapter handoff: create source commit, flush, require clean `.tandem/`, then push.", "After verified implementation is integrated, the Tandem orchestrator publishes the complete next release: version metadata and release notes, annotated and pushed Git tag, and a GitHub Release available through the normal mise source. Pi readiness is reported only after that release is live."]
  claimedAt: "2026-09-18T12:42:02Z"
  deliveredAt: "2026-09-18T12:53:18Z"
  validation: ["$ cargo test --manifest-path tandem/Cargo.toml --test checkpoint_behavior", "$ cargo test --manifest-path tandem/Cargo.toml", "$ git diff --check"]
  constraints: ["Tandem remains the sole Git writer for `.tandem/`; do not add Pi behavior, a Git hook, a push wrapper, or manual staging guidance in this repository.", "Reuse the existing checkpoint implementation and repository lock; do not create a second checkpoint engine or fake lifecycle transition.", "Never rewrite pushed commits or merges, and do not touch unrelated index/worktree state.", "The Worker must submit a plan before editing and use worker_ask for genuinely unresolved contract questions.", "The Worker owns implementation only; the Tandem orchestrator owns integration, versioning, tagging, pushing, GitHub Release publication, and final release verification.", "Do not ask the Pi workspace to implement against unreleased main or a locally copied binary."]
  summary: "Added the explicit native flush as a bare `tandem checkpoint` leaf plus global `-j/--json`. It opens the current workspace and delegates through a new `app::project::checkpoint` seam to the existing `project::checkpoint`, so it reuses the 0.13.4 engine and repository lock with no role gating, no Accord/record transition, and no document/event write. Successful results are nested at `data.checkpoint` using the same `checkpoint_json` serializer as lifecycle commands; a checkpoint failure fails closed: JSON mode prints `{\"ok\":false,\"error\":{\"code\":\"checkpoint\",\"message\":\"...\",\"details\":{\"checkpoint\":{status:\"failed\",commit:null,amended:false,consolidated:0,error:\"...\"}}}}` to stdout and exits 1, human mode reports on stderr and exits 1. Added five focused real-Git tests (absorb/preserve/no-write, idempotent + one rolling commit, merge never amended, fail-closed envelopes, leftover-chore fold) and updated the help-surface count. Docs: new `plan/task-39-pi-handoff.md` (create source commit -> flush -> require clean `.tandem/` -> push, with both envelopes), protocol/README.md now points to it and documents 25 CLI leaves, plus narrow additions to `tandem/README.md` and `docs/cli/index.md`. task-14-pi-handoff.md is untouched. No checkpoint-engine safety logic changed. Remaining gap: `docs/cli/index.md` and `tandem/README.md` still carry unrelated pre-0.3.0 staleness that this Task intentionally did not modernize."
  evidence: ["Documented native CLI operation flushes pending `.tandem/` through the existing checkpointer without creating or altering a Task, Accord, Rule, Decision, or event: `tandem checkpoint` dispatches to `app::project::checkpoint` -> `project::checkpoint`; test `explicit_checkpoint_flushes_pending_metadata_into_unpushed_source_commit` snapshots `.tandem/tasks`, `.tandem/rules`, `.tandem/events`, and `.tandem/decisions` before and after the flush and asserts byte-identical trees. It also asserts `data.checkpoint.status=checkpointed`.", "After an ordinary unpushed non-merge source commit, the flush leaves `.tandem/` clean, preserves the real commit subject, includes pending metadata, and folds leftover chores: Same test: HEAD stays subject `ordinary source commit`, HEAD^ is unchanged, `git status --porcelain -- .tandem` is empty, and the amended commit lists `.tandem/tasks/task-1.md`, `.tandem/tasks/task-2.md`, and `.tandem/events`. `explicit_checkpoint_folds_leftover_own_chore_runs` builds a chore/source/chore history and asserts consolidated >= 2, 6 commits collapse to 2, `ordinary source commit` survives, and every leftover byte remains in HEAD's tree.", "Pushed commits and merge commits are never amended; unrelated staged/unstaged/untracked files remain unchanged; metadata-only work has at most one rolling checkpoint: `explicit_checkpoint_is_idempotent_and_keeps_one_rolling_commit` proves pushed HEAD -> amended=false then amend on the next flush with commit count fixed at 2, and unpushed history keeps count 1 across repeated flushes. `explicit_checkpoint_never_amends_a_merge_head` asserts a real 3-parent-line merge HEAD yields amended=false, HEAD^ equals the merge, and count grows by exactly one. The absorb test recreates staged partial, staged deletion, and untracked state and asserts the index entry, `git diff --cached --name-status`, and worktree bytes are unchanged.", "Idempotent on clean `.tandem/` and returns status/commit/amended/consolidated plus failure details consistent with lifecycle results: Idempotent test asserts a second flush returns `status=clean`, `commit=null`, no HEAD change. Both lifecycle and explicit output call the single `checkpoint_json` serializer in tandem/src/cli/commands.rs, so the field set is shared. `explicit_checkpoint_fails_closed_with_structured_envelopes` asserts JSON failure is stdout-only, exit 1, `error.code=checkpoint`, and `details.checkpoint` has status failed, commit null, amended false, consolidated 0, and the same error text as `error.message`.", "Protocol/CLI documentation and real-Git coverage define create-source-commit -> flush -> require clean `.tandem/` -> push: protocol/README.md documents the flush, the 25-leaf command tree, the `data.checkpoint` success path, and the exit-1 checkpoint error envelope; it links plan/task-39-pi-handoff.md, which spells out the four-step handoff and both JSON envelopes; docs/cli/index.md and tandem/README.md got matching narrow additions. Coverage is `cargo test --manifest-path tandem/Cargo.toml --test checkpoint_behavior` (17 passed).", "task-14 historical contract left intact: `git show --stat HEAD` lists no change to plan/task-14-pi-handoff.md; protocol/README.md now points to task-14 for the lifecycle contract and task-39 for the flush handoff."]
  filesChanged: ["tandem/src/main.rs", "tandem/src/app/project.rs", "tandem/src/cli/model.rs", "tandem/src/cli/commands.rs", "tandem/src/cli/landing.rs", "tandem/tests/checkpoint_behavior.rs", "tandem/tests/cli_behavior.rs", "protocol/README.md", "plan/task-39-pi-handoff.md", "tandem/README.md", "docs/cli/index.md"]
  note: "The implementation boundary is sound, but two acceptance gaps need correction before integration. (1) The explicit-flush test/evidence currently proves pending Task/update/event capture but not the criterion’s pending Rule and Subtask metadata. Extend the real-Git explicit test to create a rule and direct Subtask before the flush, assert their paths/bytes are included in the amended commit, and retain the proof that checkpoint itself authors no bytes. (2) `plan/task-39-pi-handoff.md` now contradicts Algoran"
  updatedAt: "2026-09-18T12:53:18Z"
createdAt: "2026-09-18T12:41:34Z"
updatedAt: "2026-09-18T12:53:18Z"
assignee: "worker-task-39-8d6b047c"
---
Algorant approved closing the remaining gap after Tandem 0.13.4. Version 0.13.4 correctly folds pending `.tandem/` state into any unpushed non-merge real commit and reconciles leftover chores, but that logic only runs at root assignment lifecycle boundaries. `add`, `update`, rule mutations, Epic/Subtask lifecycle, and other intermediate writes can remain dirty indefinitely; ordinary `git commit && git push` does not invoke Tandem. This caused ffsync to push source commits while leaving its events log, a rule, and task-137 uncommitted.

Add one explicit native, idempotent checkpoint/flush operation that invokes the existing 0.13.4 checkpointer without fabricating an Accord or record transition. It is the authoritative integration point for adapters and commit/push workflows. The intended order is: create the real source commit, invoke native flush, verify `.tandem` clean, then push. With an unpushed non-merge real HEAD, the flush amends that commit and preserves its subject. With no absorbable real commit, metadata-only work may use at most one rolling Tandem checkpoint. This Task does not add hooks or Pi code.

This Task includes the complete Tandem release boundary, not only implementation. After the Worker delivery is independently verified and integrated by the Tandem orchestrator, prepare and publish the next Tandem release with its version metadata, annotated Git tag, pushed tag, and GitHub Release. Confirm the release is available through the normal mise source. Do not tell the Pi workspace to begin its consumer implementation merely because source landed on Tandem main; report readiness only after the full release is live. The Pi side will then install it using Algorant's exact `sysup mise` command and verify that the resolved active Tandem binary reports the new version.

Before implementation, the Worker must report a plan/spec for orchestrator approval. It should ask the orchestrator focused questions through `worker_ask` where CLI naming, envelope semantics, or safety behavior is genuinely unclear rather than guessing.
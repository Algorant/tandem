---
id: task-2
type: task
title: "CLI: update rejects decision documents"
state: "in-progress"
priority: "low"
tags: ["protocol", "papercut", "decisions"]
accord:
  status: "rework"
  acceptance: ["Common update supports active Decision documents alongside existing Tasks; status, deciders, supersedes and common title/body/tags/references/relatedFiles metadata are editable with correct type-specific validation.", "Entering accepted/rejected decision status maintains automatic decidedAt plus updatedAt according to a documented plan-approved rule; invalid statuses or inappropriate type-specific flags fail without writes.", "Repeated list flags replace lists, supported --clear operations work, unknown metadata and body bytes are preserved when unrelated, and no-op updates do not write events or timestamps.", "Decision URL/document-ID references retain task-29 semantics; repo paths can be stored as relatedFiles through decision updates without being treated as document references.", "CLI success reports actual changed fields and failures retain standard envelope/exit behavior. Regression tests exercise real native decision update, timestamp rules, lists/clears, invalid/no-op requests and existing Task update behavior."]
  claimedAt: "2026-09-11T14:27:36Z"
  deliveredAt: "2026-09-11T14:43:28Z"
  validation: ["$ just dev-check", "$ cargo fmt --manifest-path tandem/Cargo.toml --check"]
  constraints: ["Wait for task-16 to integrate before launch; it currently owns cli/commands.rs. No concurrent edits to that file.", "Protocol meaning first; app owns shared mutations, CLI dispatches by resolved document type rather than ID shape.", "Decision update only: do not expand common update to Rules or add new types/compatibility paths; do not refactor unrelated dead decision code unless directly replaced by the authoritative update path.", "Normative protocol0.3.0 and current task-29 reference semantics are authoritative; existing broader CLI docs contain stale command shapes, do not rewrite the whole docs suite.", "decidedAt records the latest actual transition into accepted or rejected and is retained on later deprecation, supersession or other metadata edits. Repeating an unchanged status is a no-op, not a backfill. Creation directly in accepted/rejected must use the same automatic timestamp policy; do not repair historical records implicitly.", "Flag shape conversion may live in CLI, but permitted mutable/clear fields and timestamp/status meaning must be validated through protocol/app, not only CLI branches. Resolve active documents under the existing mutation safety boundary.", "Emit reference/supersession warnings through JSON warnings and human stderr as appropriate; do not reproduce the existing silent human-warning defect in the new common update printer.", "Submit detailed plan and timestamp/clear/type-validation design before editing, and ask about ambiguity instead of silently choosing a divergent interpretation.", "No adapter or TUI changes."]
  summary: "Type-aware `tandem update` now supports active Decision documents alongside Tasks. The CLI resolves the target's actual `type` (never the ID prefix), rejects flags that do not apply to that type before any write, and dispatches to shared app operations. `app::decisions::update` was rewritten to edit `title`, `body`, `status`, `deciders`, `supersedes`, `references`, `relatedFiles`, and `tags`, with deterministic list replacement, supported `--clear`, reference/supersession warnings, no-op no-write detection, and the approved `decidedAt` rule: written on each actual transition into `accepted`/`rejected` (including direct creation), retained when later moving to `proposed`/`deprecated`/`superseded`, and never backfilled by repeating an unchanged status. Resolution/mutation happen under the existing `HierarchyLock` and reject archived/wrong-type targets. Human warnings now go to stderr (D55); JSON returns them in `warnings`. New native regression tests plus app unit tests cover the update surface, timestamp transitions, list/clear behavior, invalid/no-op requests, type mismatches, task-29 reference semantics, and existing Task update behavior.\n\nRemaining gaps (not defects): the broader CLI docs still contain other stale `tandem decision ...` command shapes that were out of scope; CLI `add decision` still has no `--status`, so creation directly in a terminal status is reachable only through the app API (the app fix is unit-tested)."
  evidence: ["Common update supports active Decision documents; status, deciders, supersedes, title/body/tags/references/relatedFiles editable with type-specific validation: CLI resolves project.find_document type and dispatches decision ids to app::decisions::update; native test decision_update_edits_common_metadata_and_status reports changes [title,status,decidedAt,deciders,supersedes,references,relatedFiles,tags,body] and the file contains the replaced values. Scratch repro of `update decision-1 --status accepted` succeeds (previously 'only task documents can be updated in v0').", "decidedAt + updatedAt rule; invalid statuses / inappropriate flags fail without writes: app unit test update_retains_historical_decided_at_and_repeat_status_is_a_noop pins a 2000 decidedAt, shows `--status deprecated` retains it, `--status rejected` refreshes it, and repeated `--status accepted` is change-empty with byte-identical file and no new event. Native decision_status_transitions_retain_and_refresh_decided_at repeats this at the CLI boundary. Native decision_invalid_requests_fail_without_writes asserts exit 1 for bogus status and exit 2 for `--priority` on a decision and `--status`/`--decider` on a task, with unchanged decision bytes and event bytes.", "Repeated list flags replace; supported --clear works; unknown metadata/body preserved; no-op writes no events/timestamps: decision_clear_removes_lists_and_body asserts full-list replacement and removal for tags/references/relatedFiles/deciders/supersedes/body plus `tag`/`related-file` aliases. decision_noop_and_absent_clear_write_nothing asserts byte-identical decision file and event log for an identical title and for clearing an absent list. decision_update_preserves_unknown_metadata_and_unrelated_body asserts a custom unknown field and unrelated Markdown body survive a title update.", "Task-29 reference semantics; relatedFiles not treated as document references: decision_reference_and_supersession_warnings_follow_task_29_semantics asserts a missing document ID warns, an absolute http(s) URL does not, `supersedes task-1` warns as non-decision, and `relatedFiles docs/note.md` produces no reference warning; the same update in human mode prints `Warning: reference not found: missing-doc` to stderr.", "CLI success reports changed fields; failures keep envelope/exit behavior; regression tests for real native update, timestamps, lists/clears, invalid/no-op and existing Task update: print_update_outcome emits data.changes in JSON and `Updated <id>: <fields>` / `No changes for <id>` in human mode; failures stay on the standard CliError path (exit 1 operational, 2 usage). decision_invalid_requests_fail_without_writes additionally verifies `update task-1 --title Renamed --tag cli` still updates a Task file. All 271 unit + 38 integration tests pass under just dev-check."]
  filesChanged: ["tandem/src/protocol/document.rs", "tandem/src/protocol/event.rs", "tandem/src/app/decisions.rs", "tandem/src/cli/commands.rs", "tandem/tests/decision_update_behavior.rs", "protocol/README.md", "docs/cli/index.md", "docs/guides/decisions.md"]
  note: "Owner just dev-check/fmt pass and main implementation approach is sound, but native boundary probe found a localized correctness defect: `update decision-1 --status ' accepted ' --json` exits0, stores status with literal spaces, and writes NO decidedAt (fixture /tmp/tandem-decision-owner-h788w9qn). validate_status/require_nonempty and protocol validator trim, while apply_decision_status uses original raw input. Reject noncanonical padded statuses before writes; do not silently normalize/store divergent valu"
  updatedAt: "2026-09-11T14:43:28Z"
createdAt: "2026-08-31T20:53:43Z"
updatedAt: "2026-09-11T14:43:28Z"
effort: "medium"
references: ["task-3", "task-29"]
relatedFiles: ["tandem/src/cli/commands.rs", "tandem/src/cli/model.rs", "tandem/src/app/decisions.rs", "tandem/src/app/tasks.rs", "tandem/src/protocol/document.rs", "tandem/src/protocol/event.rs", "tandem/tests/decision_update_behavior.rs", "protocol/README.md", "docs/cli/index.md", "docs/guides/decisions.md"]
assignee: "worker-task-2-a5c95c01"
---

## Description

Migrating to protocol 0.3.0 surfaced this: 'tandem update decision-8 --status accepted' fails with 'only task documents can be updated in v0'. Contract D34 requires type-aware update; decision status/deciders/supersedes must be editable through common update. Migration workaround: frontmatter authoring.

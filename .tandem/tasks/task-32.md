---
id: task-32
type: task
title: "Make legacy-rule warnings readable in the TUI Rules view"
state: "in-progress"
priority: "low"
effort: "medium"
references: ["task-12", "task-20"]
relatedFiles: ["tandem/src/tui/rules.rs", "tandem/src/tui/chrome.rs", "tandem/src/tui/mod.rs", "tandem/src/tui/reload.rs"]
tags: ["tui", "papercut", "rules"]
accord:
  status: "delivered"
  acceptance: ["At 132-column terminal width, a user in the Rules view can read or explicitly expand the full diagnostic naming tandem.md, saying embedded rules are not active, and identifying .tandem/rules/ as the correct location.", "The warning remains discoverable after ordinary view navigation and transient status expiration while the legacy block exists.", "A rendering regression test verifies visible actionable diagnostic content, not only the internal warning string; no automatic migration or activation of legacy rules is introduced."]
  claimedAt: "2026-09-11T14:22:25Z"
  deliveredAt: "2026-09-11T14:34:24Z"
  validation: ["$ just dev-check", "$ cargo fmt --manifest-path tandem/Cargo.toml --check"]
  constraints: ["TUI warning presentation only. No automatic migration, legacy rule activation, rule toggles, or app/protocol/CLI changes.", "Use a persistent visible diagnostic or clearly discoverable expanded warning in Rules view; no broad redesign of all notifications.", "Render-test actual diagnostic content and normal no-warning behavior; use valid native 0.3.0 fixture initialization for ordinary workspace tests.", "Submit implementation design and plan before editing. Parent will inspect a release TUI in a safe fixture and own preview routing."]
  summary: "Both requested corrections are implemented, validated, and committed on top of the approved banner work. Correction 1: the test helper no longer hand-builds a stale .tandem/board layout; native_workspace now calls TandemProject::initialize(root, &default_project_config(title)). The legacy fixture explicitly removes the empty native rules directory created by initialize and then injects the legacy embedded-rules config, reproducing the pre-0.3 layout for this case only. A new mixed fixture initializes natively, adds three active per-file rules through the shared app::rules::add operation, then injects the legacy config so active rules and the legacy diagnostic coexist. Correction 2: draw_rules_list now renders a compact borderless list when the list pane is shorter than MIN_RULE_LIST_HEIGHT (3). In the cramped warning layout this keeps at least one real rule row visible and keyboard-navigable while the full actionable warning is preserved; the normal no-warning layout is byte-identical because compact can only trigger when the banner consumes the rows (the list area without a banner is always >= 3 at supported sizes). Two new mixed-fixture tests cover 45x12 (alpha visible on load, Down Down scrolls charlie into view, warning still present) and 132x61 (warning tokens plus the bordered 'Always rules' list and alpha rule). Full suite is 276 bin tests plus 5 integration suites green, with both native commands exiting 0 on the committed tree. Remaining gap unchanged: parent-owned release-TUI ANSI inspection; I did not touch preview routing or the /tmp/tandem-closeout.I7vnyI/legacy fixture."
  evidence: ["Native 0.3.0 fixture requirement; no hand-built stale layout fixtures.: native_workspace now returns TandemProject::initialize(root, &crate::protocol::config::default_project_config(title)).unwrap(). legacy_rules_workspace wraps it, calls fs::remove_dir_all(workspace.rules_dir()).unwrap(), then writes LEGACY_RULES_CONFIG. mixed_rules_workspace wraps native init, adds three rules via crate::app::rules::add, then writes LEGACY_RULES_CONFIG.", "At 132 columns the full diagnostic remains readable in Rules view.: rules_view_renders_legacy_rules_diagnostic_at_132_columns (status cleared) shows tandem.md, 'not active', and .tandem/rules/; rules_view_mixed_rules_render_warning_and_rules_at_normal_width additionally shows the 'Always rules' bordered list and 'alpha active rule' with the banner present.", "Mixed active + legacy rules keep the selected real rule visibly navigable at minimum size while preserving the warning.: rules_view_mixed_rules_keep_selected_rule_at_minimum_size at 45x12 shows the warning tokens plus 'alpha active rule'; after two Down presses the compact borderless list scrolls 'charlie active rule' into view and the warning tokens remain.", "Warning persists after navigation and transient status expiration.: rules_view_legacy_rules_diagnostic_persists_after_status_expiry navigates Rules->Board->Rules, asserts expire_transient_status() clears app.status, then redraws 132x61 with all three tokens present.", "No stale banner state when the legacy block is removed.: rules_view_legacy_rules_diagnostic_clears_when_block_is_removed reloads a native config over the legacy one, asserts rules_warnings is empty, and confirms no '.tandem/rules/' text remains in a 132x61 redraw.", "Normal no-warning layout unchanged.: draw_rules_view keeps [Length(1), Min(3)] when rules_warnings is empty; draw_rules_list only takes the borderless path when area.height < MIN_RULE_LIST_HEIGHT (3), which cannot occur without the banner at supported sizes. rules_view_without_legacy_block_has_no_diagnostic_banner remains green.", "No automatic migration or activation of legacy rules.: The legacy-only 132-column test asserts app.workspace.rules_dir() does not exist and app.rules has no entries; the presentation path only reads diagnostics and never writes or activates legacy embedded rules."]
  filesChanged: ["tandem/src/tui/mod.rs", "tandem/src/tui/rules.rs", "tandem/src/tui/reload.rs"]
  updatedAt: "2026-09-11T14:34:24Z"
createdAt: "2026-09-11T13:35:26Z"
updatedAt: "2026-09-11T14:34:24Z"
assignee: "worker-task-32-7222a706"
---

## Description

Found during Algorant-requested closeout of task-12 on 0.13.1. Algorant explicitly approved closing the shipped detection fix and tracking this presentation limitation separately.

Reproduction: a valid 0.3.0 workspace with a populated legacy rules block in tandem.md and no rules directory. CLI human/JSON correctly says the rules are not active and directs them to .tandem/rules/. The TUI reload also generates the same warning, but in the Rules view at 132x61 the normal footer hints consume most of the row: immediately after r, only an orange 'Reloaded 0 active documents from .tand...' suffix is visible. After the transient status expires, the view shows Rules (0) with no actionable diagnostic.

Existing reload_warns_for_legacy_embedded_rules asserts ReloadOutcome.first_warning, not rendered visibility. Add real rendering coverage. Temporary reproduction script and ANSI captures: /tmp/tandem-closeout.I7vnyI/verify.sh and legacy-reload.ansi (ephemeral evidence; reproduction described here is durable).

Scope is warning presentation only, not migration or compatibility loading. Choose the smallest readable presentation in the Rules view; do not redesign all TUI notifications as part of this papercut.

{
  "name": "tandem-docs-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "npm run sync:docs",
    "dev": "astro dev",
    "prestart": "npm run sync:docs",
    "start": "astro dev",
    "prebuild": "npm run sync:docs",
    "build": "astro build",
    "preview": "astro preview",
    "sync:docs": "node scripts/sync-docs.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.1",
    "astro": "^7.0.3",
    "sharp": "^0.35.2"
  }
}

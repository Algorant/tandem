# Tandem docs site

Astro Starlight project for rendering the repository-level Tandem documentation.

Canonical Markdown source lives in `../docs/`. The site project owns rendering, navigation, and static build tooling.
